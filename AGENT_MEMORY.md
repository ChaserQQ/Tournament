# Agent Memory

This file is the durable project memory for coding agents. Read it before
continuing long-running work, after context compaction, or when a task depends on
GitHub, Firebase, deployment, or prior cleanup decisions.

## Project identity
- Verified 2026-06-13 KST: local workspace is `C:\Users\rlaal\Desktop\mtm`.
- Verified 2026-06-13 KST: GitHub repository is `ChaserQQ/Tournament`.
- Verified 2026-06-13 KST: primary branch is `main`.
- Verified 2026-06-13 KST: public GitHub Pages URL is `https://chaserqq.github.io/Tournament/`.
- Local support skill: `C:\Users\rlaal\.codex\skills\project-memory-keeper`.

## Standing user instructions
- Prefer Korean for user-facing explanations.
- Do not start code changes until the user asks for work.
- After repository code changes, validate, commit, push to GitHub `main`, and verify the deployment when feasible.
- Keep GitHub history as a latest-snapshot-only branch until the user changes this policy: amend/recreate the current root commit and force-push with lease so `main` continues to expose only the latest version.
- Continue version/cache updates for every public app change: increment `src/core/build.js` build metadata and the matching `index.html` asset query strings so GitHub Pages serves the latest files.
- Firebase direct edits are allowed only when the user explicitly asks for them or the task clearly requires them.
- Keep summaries concrete: mention exact files, validation commands, deployment status, and unresolved risks.

## Current verified state
- Verified 2026-06-13 KST before this memory file was added: baseline commit `49d920c Remove obsolete patch layers`.
- Verified 2026-06-13 KST after UI foundation work: `src/core/build.js` reports `version: 176` and label `BUILD v176 UI FOUNDATION PASS`.
- Verified 2026-06-14 KST after admin mobile list work: `src/core/build.js` reports `version: 177` and label `BUILD v177 ADMIN MOBILE LIST POLISH`.
- Verified 2026-06-14 KST after operator session lease work: `src/core/build.js` reports `version: 178` and label `BUILD v178 OPERATOR SESSION LEASE`.
- Verified 2026-06-14 KST after legacy patch cleanup work: `src/core/build.js` reports `version: 179` and label `BUILD v179 LEGACY PATCH CLEANUP`.
- Verified 2026-06-14 KST after button alignment work: `src/core/build.js` reports `version: 180` and label `BUILD v180 BUTTON ALIGNMENT`.
- Verified 2026-06-14 KST after legacy runtime trim work: `src/core/build.js` reports `version: 181` and label `BUILD v181 LEGACY RUNTIME TRIM`.
- Verified 2026-06-14 KST after checkbox size fix work: `src/core/build.js` reports `version: 182` and label `BUILD v182 CHECKBOX SIZE FIX`.
- Verified 2026-06-14 KST after mobile ops dock work: `src/core/build.js` reports `version: 183` and label `BUILD v183 MOBILE OPS DOCK`.
- Verified 2026-06-14 KST after live route polish work: `src/core/build.js` reports `version: 184` and label `BUILD v184 LIVE ROUTE POLISH`.
- Verified 2026-06-14 KST after runtime cleanup work: `src/core/build.js` reports `version: 185` and label `BUILD v185 RUNTIME CLEANUP`.
- Verified 2026-06-14 KST: build asset versions are `config: 156`, `build: 185`, `app: 185`, `css: 184`, `og: 51`.
- Verified 2026-06-14 KST after Git history reset: remote `main` was rewritten to a latest-snapshot-only root commit and old remote `codex/*` branches plus the old `MINI4WD` tag were removed.
- Treat commit hashes, Pages run IDs, Firebase state, and public asset responses as volatile. Re-verify them from source before relying on them.

## Validation checklist
- Run `npm run verify` after meaningful code or CSS edits.
- Run `git diff --check` or `git diff --cached --check` before committing.
- Run `tools/qa-surface-check.cjs` with Playwright when checking public routes, auth guards, LIVE lobby, TV fallback, same-tab stale fallback, and mobile overflow.
- Run `tools/qa-operator-flow.cjs` with Playwright when checking fake-auth operator flow, points5Tree start/scoring, mobile operator layout, DB route, and dashboard route without touching live Firebase.
- For frontend changes, test representative surfaces locally through a static server such as `http://127.0.0.1:8765/`.
- Representative browser checks: login, live lobby, TV live, invalid share/error route, and mobile login.
- After push, watch the GitHub Pages workflow with `gh run watch <run_id> --repo ChaserQQ/Tournament --exit-status` when a run is available.
- After Pages deploys, verify the public URL and versioned assets, not only the local build.

## Firebase and data safety
- Never store secrets, auth codes, raw Firebase exports, credentials, or private user data in this file.
- Do not commit Firebase exports or rule dumps unless the user explicitly requests a sanitized artifact.
- For Firebase fixes, document the intended path-level change and validation result instead of copying private data.

## Recent durable changes
- 2026-06-28: added full actual-match simulation QA after the user requested checking every match mode end-to-end, not only targeted local flow checks. `tools/qa-match-simulation.cjs` runs real browser/operator-state simulations with Firebase stubs for `basic`, `points3`, `points5Tree`, `revival`, and `crow` across desktop 1365x900 and mobile 390x844, driving the same app functions used by the operator screen from tournament setup through finalist/final/winner selection and `finishTournament()` history save. `package.json` adds `npm.cmd run qa:match`, and `tools/verify-static.js` now requires the full-match simulation guard. Validation passed: `node --check tools/qa-match-simulation.cjs`, `npm.cmd run verify`, `npm.cmd run qa:audit`, `npm.cmd run qa:match` with 0 failures and record rows saved for every mode, `npm.cmd run qa:result`, `tools/qa-operator-flow.cjs`, and `git diff --check`.
- 2026-06-27: v270 live lease retry fixes the operator refresh case where the screen could show a LIVE/operator-rights checking message before the v269 publicLive republish ran. `src/app.js` now makes background LIVE sync reasons wait for the v178 operator lease and retry silently through `ensureWritableLeaseForBackgroundSyncV270`, prevents `publishLiveStateFallbackV269` from bypassing another browser's active lease via `__mini4wdCanPublishLiveNowV270`, and routes render/queued background saves through lease acquisition without user-facing alerts. Build metadata is version 270, label BUILD v270 LIVE LEASE RETRY, assets build/app 270. Validation passed: `node --check` for changed JS files, `npm.cmd run verify`, `npm.cmd run qa:audit`, and `tools/qa-operator-flow.cjs` across 1365/390/320 viewports with 0 failures, including `liveLeaseRetryV270` with alert count 0, public live published, and `broadcast.roundIndex: 1`.
- 2026-06-27: v269 live publish fallback fixes the case where TV/mobile LIVE stayed on the 1st-round point final after the operator had advanced to the next round. `src/app.js` now routes critical operator live-sync calls through `syncOperatorLiveStateV269`, adds `publishLiveStateFallbackV269` to write `tournaments/{id}/state` and `publicLive/{id}` in one update when the normal sync path returns false, and schedules a throttled operator-render live republish so a refreshed operator screen can repair stale public LIVE state without another button press. `tools/qa-operator-flow.cjs` adds `liveRoundFallbackV269`, proving that after a 1st-round point finalist is confirmed, local and public LIVE both move to `activeRoundIndex: 1` and `broadcast.roundIndex: 1`. Build metadata is version 269, label BUILD v269 LIVE PUBLISH, assets build/app 269. Validation passed: `node --check` for changed JS files, `npm.cmd run verify`, `npm.cmd run qa:audit`, `tools/qa-operator-flow.cjs` across 1365/390/320 viewports with 0 failures, `git diff --check`, GitHub Pages run 28291465380, and public v269 index/build/app asset checks. Verified after deploy that the live Firebase `publicLive/아테네월드-260627-오픈상패전-20260627` source was still stale at `activeRoundIndex: 0` until the authenticated operator browser reloads v269 and republishes its current local state.
- 2026-06-26: v255 CSS audit cleanup was found during the daily project health check. `npm.cmd run qa:audit` failed on stale CSS-only operator queue/controller selectors left after the v246/v248 operator UI removals: `operator-queue-panel-v224`, `operator-queue-empty-v224`, `operator-queue-list-v224`, `operator-queue-item-v224`, `operator-controller-head-v226`, `operator-queue-panel-v227`, `operator-queue-list-v227`, `operator-queue-item-v227`, and `operator-queue-empty-v227`. `src/styles/app.css` removes those stale selector blocks and combined-selector references, and `tools/verify-static.js` now blocks the selector set from returning. Build metadata is now version 255, label BUILD v255 CSS AUDIT CLEANUP, assets build/css 255 and app 254. Validation passed: `node --check src/core/build.js`, `node --check tools/verify-static.js`, `npm.cmd run verify`, `npm.cmd run qa:audit` with `cssOnlyVersionedClasses: 0`, `tools/qa-operator-flow.cjs`, `tools/qa-admin-flow.cjs`, `tools/qa-result-matrix.cjs`, `tools/qa-surface-check.cjs` with temp Playwright `NODE_PATH` and Edge `MINI4WD_BROWSER_PATH`, and `git diff --check`. Force-pushed the current root snapshot to `main`, GitHub Pages run `28184021617` passed, and public checks confirmed v255 index/build/css with app.js intentionally still at v254 and stale selectors absent from public CSS.
- 2026-06-25: v254 operator rhythm rules responds to the user request to add rules so all frames/windows align left/right, same-type vertical gaps match, and button padding stays consistent with nearby buttons. `src/styles/app.css` adds a v254 operator rhythm contract with shared frame gap and button padding variables, enforces full-width stack/side frames, and removes the remaining primary dock padding exception while preserving smaller primary label sizing. `tools/qa-operator-flow.cjs` adds `operatorFrameRhythmV254`, measuring task/side frame left/right/width deltas, same-stack vertical gap deltas, and padding-set mismatches inside operator button groups. `tools/verify-static.js` requires the v254 CSS and QA guards. Build metadata is now version 254, label BUILD v254 OPERATOR RHYTHM RULES, assets build/app/css 254. Validation passed: `node --check src/core/build.js`, `node --check tools/verify-static.js`, `node --check tools/qa-operator-flow.cjs`, `npm.cmd run verify`, `tools/qa-operator-flow.cjs` with temp Playwright `NODE_PATH` across 3 viewports with 0 failures, and `git diff --check`. Browser QA measured mobile task/side stacks with left/right/width deltas 0, same-stack gaps 8px, dock widths 85/85/85/85 at 390px and 68/68/68/68 at 320px, and no bad button padding groups. GitHub Pages deployment passed after force push, and public index/build/app/css v254 asset checks passed.
- 2026-06-25: v253 operator dock equal columns responds to the screenshot showing the first mobile dock button background much wider than the other three. `src/styles/app.css` adds a v253 mobile dock rule that changes `operator-mobile-dock-v233` from the earlier wide-primary grid to four equal `1fr` columns while preserving the v251/v252 height, centering, and background contracts. The primary dock button keeps a smaller 11px label and tighter inline padding so its longer copy fits the equal-width cell better. `tools/qa-operator-flow.cjs` adds `operatorMobileDockWidthV253`, checking all four dock button background rect widths and inter-button gaps; `tools/verify-static.js` requires the v253 CSS and QA guards. Build metadata is now version 253, label BUILD v253 OPERATOR DOCK EQUAL COLUMNS, assets build/app/css 253. Validation passed: `node --check src/core/build.js`, `node --check tools/verify-static.js`, `node --check tools/qa-operator-flow.cjs`, `npm.cmd run verify`, `tools/qa-operator-flow.cjs` with temp Playwright `NODE_PATH` across 3 viewports with 0 failures, and `git diff --check`. The browser QA measured mobile dock widths as 85/85/85/85 at 390px and 68/68/68/68 at 320px, with equal 6px gaps. GitHub Pages deployment passed after force push, and public index/build/app/css v253 asset checks passed.
- 2026-06-25: v252 operator dock background align responds to the user clarification that the remaining mobile dock issue was the button backgrounds, not just text/box height. `src/styles/app.css` adds a v252 mobile-dock background layer that removes inherited gradients/images, pins the dock container to the shared surface, gives every secondary dock button the same soft background and line color, and keeps the primary button as a solid accent background on the same framed area. `tools/qa-operator-flow.cjs` adds `operatorMobileDockBackgroundV252`, checking that dock buttons have no background images, no transparent backgrounds, border-box background clip/origin, one shared secondary background, and one shared secondary border. `tools/verify-static.js` requires the v252 CSS and QA guards. Build metadata is now version 252, label BUILD v252 OPERATOR DOCK BACKGROUND ALIGN, assets build/app/css 252. Validation passed: `node --check src/core/build.js`, `node --check tools/verify-static.js`, `node --check tools/qa-operator-flow.cjs`, `npm.cmd run verify`, `tools/qa-operator-flow.cjs` with temp Playwright `NODE_PATH` across 3 viewports with 0 failures, and `git diff --check`. GitHub Pages deployment passed after force push, and public index/build/app/css v252 asset checks passed.
- 2026-06-25: v251 operator dock optical align responds to the user report that the mobile operator dock button positions and heights still looked mismatched. `src/styles/app.css` adds a v251 mobile-dock layer that forces every `operator-mobile-dock-v233` button, including the first primary action, onto the same 40px grid track with equal padding, shared font metrics, centered grid placement, no transform, and preserved primary color. `tools/qa-operator-flow.cjs` adds `operatorMobileDockOpticalV251`, measuring text-line count, top/bottom padding symmetry, grid placement, and text-center offset in addition to the existing outer button rect checks. `tools/verify-static.js` requires the v251 CSS and QA guards. Build metadata is now version 251, label BUILD v251 OPERATOR DOCK OPTICAL ALIGN, assets build/app/css 251. Validation passed: `node --check src/core/build.js`, `node --check tools/verify-static.js`, `node --check tools/qa-operator-flow.cjs`, `npm.cmd run verify`, `tools/qa-operator-flow.cjs` with temp Playwright `NODE_PATH` across 3 viewports with 0 failures, and `git diff --check`. GitHub Pages deployment passed after force push, and public index/build/app/css v251 asset checks passed.
- 2026-06-25: v250 operator compact align responds to the user request to inspect and fix awkward two-line labels, unnecessary whitespace, and horizontal/vertical alignment on the operator surface. `src/styles/app.css` adds a v250 compact rhythm layer that hides the round-tab status subtitles, keeps round/final/point/ops buttons in one-line centered rows, tightens operator header/overview/task/side-panel gaps, and restores just enough mobile bottom clearance for the fixed dock. `tools/qa-operator-flow.cjs` adds `operatorCompactRhythmV250`, measuring visible button wrap/height, round-tab height, task-stack gaps, and header height across browser QA; `tools/verify-static.js` requires the v250 CSS and QA markers. Build metadata is now version 250, label BUILD v250 OPERATOR COMPACT ALIGN, assets build/app/css 250. Validation passed: `node --check src/core/build.js`, `node --check tools/verify-static.js`, `node --check tools/qa-operator-flow.cjs`, `npm.cmd run verify`, `tools/qa-operator-flow.cjs` with temp Playwright `NODE_PATH` across 3 viewports with 0 failures, and `git diff --check`. GitHub Pages deployment passed after force push, and public index/build/app/css v250 asset checks passed.
- 2026-06-24: v249 operator header polish responds to the user request to make the operator titlebar look better and follows up the adjacent border/dock alignment complaints. `src/app.js` marks the operator titlebar with `operator-titlebar-v249`; `src/styles/app.css` adds a v249 layer that gives the titlebar a tighter 8px framed command-surface treatment with an accent rail and operation pill, pins the `emergency-adjust-card-v99` participant adjustment panel back to the shared 8px operator border contract, and forces the mobile dock buttons to the same 38px centered grid alignment. `tools/verify-static.js` requires the v249 app/CSS/QA markers, and `tools/qa-operator-flow.cjs` measures `operatorHeaderPolishV249`, `operatorEmergencyPanelV249`, and `operatorMobileDockAlignmentV249` in browser QA. Build metadata is now version 249, label BUILD v249 OPERATOR HEADER POLISH, assets build/app/css 249. Validation passed: `node --check src/app.js`, `node --check src/core/build.js`, `node --check tools/verify-static.js`, `node --check tools/qa-operator-flow.cjs`, `npm.cmd run verify`, and `tools/qa-operator-flow.cjs` with temp Playwright `NODE_PATH` and Edge `MINI4WD_BROWSER_PATH`. GitHub Pages deployment passed after force push, and public index/build/app/css v249 asset checks passed.
- 2026-06-24: v248 operator queue trim responds to the user request to delete the duplicated `대기열` panel below the point-stage area. `src/app.js` no longer calls or defines `renderOperatorQueuePanelV224()`, so the operator task stack keeps the round buttons, final shortcut, current stage, and final panel without rendering the separate queue card. `tools/verify-static.js` now blocks `operator-queue-panel-v227` from returning to `src/app.js`, and `tools/qa-operator-flow.cjs` checks `operatorQueuePanelGoneV248` in browser QA. Build metadata is now version 248, label BUILD v248 OPERATOR QUEUE TRIM, assets build/app/css 248. Validation passed: `node --check src/app.js`, `node --check src/core/build.js`, `node --check tools/verify-static.js`, `node --check tools/qa-operator-flow.cjs`, `npm.cmd run verify`, and `tools/qa-operator-flow.cjs` with temp Playwright `NODE_PATH` and Edge `MINI4WD_BROWSER_PATH`. GitHub Pages deployment passed after force push, and public index/build/app v248 asset checks passed.
- 2026-06-24: v247 point stage header trim responds to the user request to delete the point-stage header card that showed `포인트 1차전`, `TV 송출중`, group count, and score-rule copy. `src/app.js` now renders regular point stages as `point-stage-trim-v247` without the `operator-stage-head-v227` header block, leaving the point group cards and scoring controls intact. `tools/verify-static.js` blocks the deleted point score-rule header from returning, and `tools/qa-operator-flow.cjs` measures `operatorPointStageTrimV247` during point-round QA to ensure no stage header, broadcast copy, or point-rule copy remains inside the point stage card. Build metadata is now version 247, label BUILD v247 POINT STAGE HEADER TRIM, assets build/app/css 247. Validation passed: `node --check src/app.js`, `node --check src/core/build.js`, `node --check tools/verify-static.js`, `node --check tools/qa-operator-flow.cjs`, `npm.cmd run verify`, and `tools/qa-operator-flow.cjs` with temp Playwright `NODE_PATH` and Edge `MINI4WD_BROWSER_PATH`. GitHub Pages deployment passed after force push, and public index/build/app v247 asset checks passed.
- 2026-06-24: v246 merged current controls responds to the user request to combine the duplicated operator overview/current-game areas into one. `src/app.js` now renders the current group/progress detail and `renderOperatorControlConsoleV226()` inside `operator-overview-v226`, removes the standalone task-stack controller and the duplicate `현재 경기` header, and keeps `operator-feedback-v228` adjacent to the merged controls. `src/styles/app.css` adds a v246 overview-merge layer so the action/group-size controls span the same top overview card without another framed panel. `tools/verify-static.js` blocks the old duplicate header from returning, and `tools/qa-operator-flow.cjs` measures `operatorMergedOverviewV246` to ensure the controls and feedback are in the overview, the standalone controller count is zero, and stage progress/current group details remain visible. Build metadata is now version 246, label BUILD v246 MERGED CURRENT CONTROLS, assets build/app/css 246. Validation passed: `node --check src/app.js`, `node --check src/core/build.js`, `node --check tools/verify-static.js`, `node --check tools/qa-operator-flow.cjs`, `npm.cmd run verify`, `tools/qa-operator-flow.cjs` with temp Playwright `NODE_PATH` and Edge `MINI4WD_BROWSER_PATH`, and `git diff --check`. GitHub Pages deployment passed after force push, and public index/build/app/css v246 asset checks passed.
- 2026-06-24: v245 final shortcut responds to the user request to move the `최종 결승 진행` action directly under the 1/2/3 round buttons and remove the rest of the pending final-summary area. `src/app.js` now renders `operator-final-shortcut-v245` immediately after `operator-round-rail-v226`, suppresses `renderOperatorFinalPanelV224()` until a final race exists, and returns no old pending `final-box` summary when `state.finalRace` is empty. `src/styles/app.css` adds a v245 shortcut row that spans the same left/right edges as the round rail. `tools/verify-static.js` blocks the old pending final summary card, and `tools/qa-operator-flow.cjs` measures `operatorFinalShortcutV245` to ensure the shortcut is below the round tabs, aligned, and the old pending final box count is zero. Build metadata is now version 245, label BUILD v245 FINAL SHORTCUT, assets build/app/css 245. Validation passed: `node --check src/app.js`, `node --check src/core/build.js`, `node --check tools/verify-static.js`, `node --check tools/qa-operator-flow.cjs`, `npm.cmd run verify`, `tools/qa-operator-flow.cjs` with temp Playwright `NODE_PATH` and Edge `MINI4WD_BROWSER_PATH`, and `git diff --check`. GitHub Pages deployment passed after force push, and public index/build/app/css v245 asset checks passed.
- 2026-06-24: v244 CSS audit cleanup was found during the daily project health check. `src/styles/app.css` removes the stale `.operator-mobile-nav-v224` CSS-only selector block now superseded by the v233 mobile top route/dock navigation, and adds a v244 marker. `tools/verify-static.js` now blocks the stale selector from returning. Build metadata is now version 244, label BUILD v244 CSS AUDIT CLEANUP, assets build/app/css 244. Local validation passed: `node --check src/app.js`, `node --check src/core/build.js`, `node --check tools/verify-static.js`, `node --check tools/audit-code-css.cjs`, `node --check tools/qa-operator-flow.cjs`, `npm.cmd run verify`, `npm.cmd run qa:audit`, `tools/qa-operator-flow.cjs`, `tools/qa-admin-flow.cjs`, `tools/qa-result-matrix.cjs`, `tools/qa-surface-check.cjs` with temp Playwright `NODE_PATH` and Edge `MINI4WD_BROWSER_PATH`, and `git diff --check`.
- 2026-06-24: v243 side panel align responds to the user request to match the operator setup/tools panel headers and align action buttons to the same left/right edges inside each framed area. `src/styles/app.css` adds a v243 operator side-panel layer that gives static panel headers one shared height/background/bottom border and makes `btnrow`, manual help, download, and ops button rows fill the section content width. `src/app.js` wraps the two record-download buttons in `operator-download-button-row-v243`, and `tools/qa-operator-flow.cjs` measures header/button edge alignment with `operatorSideAlignmentV243`. Build metadata is now version 243, label BUILD v243 SIDE PANEL ALIGN, assets build/app/css 243. Validation passed: `node --check src/app.js`, `node --check src/core/build.js`, `node --check tools/verify-static.js`, `node --check tools/qa-operator-flow.cjs`, `npm.cmd run verify`, `tools/qa-operator-flow.cjs` with temp Playwright `NODE_PATH` and Edge `MINI4WD_BROWSER_PATH`, and `git diff --check`. GitHub Pages deployment passed after force push, and public index/build/app/css v243 asset checks passed.
- 2026-06-24: v242 live panel trim responds to the user request to remove the whole `라이브 송출` card from the operator tools panel. `src/app.js` no longer renders `renderFirebaseLivePanel()` or `live-sync-panel` inside `operatorOpsAreaV183`, while keeping the existing live lobby route and broadcast sync logic intact. `tools/verify-static.js` blocks the removed panel from returning, and `tools/qa-operator-flow.cjs` checks that the operator tools panel has no live broadcast card/copy. Build metadata is now version 242, label BUILD v242 LIVE PANEL TRIM, assets build/app/css 242. Validation passed: `node --check src/app.js`, `node --check src/core/build.js`, `node --check tools/verify-static.js`, `node --check tools/qa-operator-flow.cjs`, `npm.cmd run verify`, `tools/qa-operator-flow.cjs` with temp Playwright `NODE_PATH` and Edge `MINI4WD_BROWSER_PATH`, and `git diff --check`. GitHub Pages deployment passed after force push, and public index/build/app/css v242 asset checks passed.
- 2026-06-24: v241 group copy responds to the user request to change the operator setting label from `조 수` to `조 편성`. `src/app.js` now renders the forced group-count setting as `조 편성`, `tools/verify-static.js` blocks the old exact label from returning, and `tools/qa-operator-flow.cjs` samples the operator setup panel text in browser QA. Build metadata is now version 241, label BUILD v241 GROUP COPY, assets build/app/css 241. Validation passed: `node --check src/app.js`, `node --check src/core/build.js`, `node --check tools/verify-static.js`, `node --check tools/qa-operator-flow.cjs`, `npm.cmd run verify`, `tools/qa-operator-flow.cjs` with temp Playwright `NODE_PATH` and Edge `MINI4WD_BROWSER_PATH`, and `git diff --check`. GitHub Pages deployment passed after force push, and public index/build/app/css v241 asset checks passed.
- 2026-06-24: v240 mobile ready trim responds to the user request to remove the duplicate mobile ready-state current-game area and its duplicate `선수 명단`/`설정 열기` buttons. `src/app.js` now marks the mobile no-current-stage operator control console with `operator-ready-console-v240`, `hidden`, and `aria-hidden`, while keeping the console available on desktop and after a stage exists. `src/styles/app.css` forces that mobile ready console hidden so older display rules cannot override it. `tools/qa-operator-flow.cjs` samples `operatorReadyConsoleVisibleV240` and fails when the mobile ready current-game console is visible. `tools/verify-static.js` requires the v240 app/CSS/QA guards. Build metadata is now version 240, label BUILD v240 MOBILE READY TRIM, assets build/app/css 240. Validation passed: `node --check src/app.js`, `node --check src/core/build.js`, `node --check tools/verify-static.js`, `node --check tools/qa-operator-flow.cjs`, `npm.cmd run verify`, `tools/qa-operator-flow.cjs` with temp Playwright `NODE_PATH` and Edge `MINI4WD_BROWSER_PATH`, and `git diff --check`. GitHub Pages deployment passed after force push, and public index/build/app/css v240 asset checks passed.
- 2026-06-23: v239 point final trim responds to the user request to remove the visible `포인트 상위 3명 결정전` header area and same-team notice from the operator point-final screen. `src/app.js` now marks `pointFinal` stages with `point-final-stage-v239`, hides that stage header/meta block, and suppresses the `같은 팀 일부 배정` warning for point-final stages while leaving the group cards and scoring controls visible. `tools/qa-operator-flow.cjs` adds the `point-final-trim` browser guard that renders a point-final stage with same-team metadata and fails if the removed title/header/same-team copy is visible. `tools/verify-static.js` requires the v239 app and QA guard. Build metadata is now version 239, label BUILD v239 POINT FINAL TRIM, assets build/app/css 239. Validation passed: `node --check src/app.js`, `node --check src/core/build.js`, `node --check tools/verify-static.js`, `node --check tools/qa-operator-flow.cjs`, `npm.cmd run verify`, `tools/qa-operator-flow.cjs` with temp Playwright `NODE_PATH` and Edge `MINI4WD_BROWSER_PATH`, and `git diff --check`. GitHub Pages deployment passed after force push, and public index/build/app/css v239 asset checks passed.
- 2026-06-23: v238 operator dock height responds to the user request to reduce the mobile dock next-game/primary button to match the other dock buttons. `src/styles/app.css` now fixes every `.operator-mobile-dock-v233` button, including the first child primary action, to 38px height/min-height/max-height while preserving the primary color. `tools/qa-operator-flow.cjs` now samples `operatorMobileDockButtonHeights` and fails when mobile dock button heights differ or exceed 40px. `tools/verify-static.js` requires the v238 marker and QA sampling. Build metadata is now version 238, label BUILD v238 OPERATOR DOCK HEIGHT, assets build/app/css 238. Validation passed: `node --check src/app.js`, `node --check src/core/build.js`, `node --check tools/verify-static.js`, `node --check tools/qa-operator-flow.cjs`, `npm.cmd run verify`, `tools/qa-operator-flow.cjs` with temp Playwright `NODE_PATH` and Edge `MINI4WD_BROWSER_PATH`, and `git diff --check`. GitHub Pages deployment passed after force push, and public index/build/app/css v238 asset checks passed.
- 2026-06-23: v237 operator misc copy responds to the user request to rename the operator `도구` area and clean up remaining copy inside it. `src/app.js` now shows the side panel title and mobile dock label as `기타`; the panel internals now use `안내와 기록`, `안내 열기`, and `기록 저장` instead of `작업`, `도움말`, and `기록 내려받기`. The legacy v211 command group label was also changed to `기타` so old operator command rendering cannot reintroduce visible `도구`. `tools/qa-operator-flow.cjs` now expects the mobile dock labels `경기`/`설정`/`기타` and fails on visible standalone `도구`, `작업`, `도움말`, or `기록 내려받기`. `tools/verify-static.js` requires the v237 copy and marker. Build metadata is now version 237, label BUILD v237 OPERATOR MISC COPY, assets build/app/css 237. Validation passed: `node --check src/app.js`, `node --check src/core/build.js`, `node --check tools/verify-static.js`, `node --check tools/qa-operator-flow.cjs`, `npm.cmd run verify`, `tools/qa-operator-flow.cjs` with temp Playwright `NODE_PATH` and Edge `MINI4WD_BROWSER_PATH`, and `git diff --check`. GitHub Pages deployment passed after force push, and public index/build/app/css v237 asset checks passed with old tool copy absent from public app.js.
- 2026-06-23: v236 operator static panels responds to the user instruction not to make panels fold like tools. `src/app.js` now renders `operatorSetupAreaV214` and `operatorOpsAreaV183` as always-visible `section.operator-static-panel-v236` panels instead of `details/summary`, and `scrollOperatorSectionV147()` only scrolls to those panels without opening a disclosure. `src/styles/app.css` adds the v236 marker and direct header rules so the v235 border contract still applies without summary selectors. `tools/verify-static.js` blocks `details.operator-page` from returning in app DOM, and `tools/qa-operator-flow.cjs` checks for two static operator panels and no folding operator details. Build metadata is now version 236, label BUILD v236 OPERATOR STATIC PANELS, assets build/app/css 236. Validation passed: `node --check src/app.js`, `node --check src/core/build.js`, `node --check tools/verify-static.js`, `node --check tools/qa-operator-flow.cjs`, `npm.cmd run verify`, `tools/qa-operator-flow.cjs` with temp Playwright `NODE_PATH` and Edge `MINI4WD_BROWSER_PATH`, and `git diff --check`. GitHub Pages deployment passed after force push, and public index/build/app/css v236 asset checks passed with `details.operator-page` absent from public app.js.
- 2026-06-23: v235 operator border contract responds to the user complaint that even the operator screen borders were still not unified. `src/styles/app.css` now keeps the v233/v234 navigation and dock behavior but applies one operator border contract across the visible operator frames: overview, round rail, round tabs, controller, current match, queue, setup/tools panels, final box, and mobile dock all resolve to an 8px radius and 1px shared line. Strong ID selectors pin `#operatorCurrentRoundV147`, `#operatorSetupAreaV214`, and `#operatorOpsAreaV183` so older `round-card` and `operator-page` rules cannot win with larger radii. `tools/qa-operator-flow.cjs` now samples `operatorBorderSample` for the major operator frames and fails when radius/border diverge; `tools/verify-static.js` requires the v235 marker, strong selectors, and QA sampling. Build metadata is now version 235, label BUILD v235 OPERATOR BORDER CONTRACT, assets build/app/css 235. Validation passed: `node --check src/app.js`, `node --check src/core/build.js`, `node --check tools/verify-static.js`, `node --check tools/qa-operator-flow.cjs`, `npm.cmd run verify`, `tools/qa-operator-flow.cjs` with Codex `NODE_PATH`, `git diff --check`, and `git diff --cached --check`. GitHub Pages deployment passed after force push, and public index/build/app/css v235 asset checks passed.
- 2026-06-23: v234 operator dock border responds to the user report that the lower fixed dock still had a different border shape from the rest of the screen. `src/styles/app.css` keeps the v233 top-route/bottom-dock structure but flattens `operator-mobile-dock-v233` to the compact shared panel style: 8px radius, 1px `var(--v218-line)` border, white surface, low shadow, no backdrop blur, and 6px child buttons with bordered flat backgrounds. `tools/verify-static.js` now requires the v234 CSS marker, and `tools/qa-operator-flow.cjs` checks mobile dock radius, border, backdrop, height, top routes, and one-row dock labels. Build metadata is now version 234, label BUILD v234 OPERATOR DOCK BORDER, assets build/app/css 234. Validation passed: `node --check src/app.js`, `node --check src/core/build.js`, `node --check tools/verify-static.js`, `node --check tools/qa-operator-flow.cjs`, `npm.cmd run verify`, `tools/qa-operator-flow.cjs` with Codex `NODE_PATH`, `git diff --check`, and `git diff --cached --check`. GitHub Pages deployment passed after force push, and public index/build/app/css v234 asset checks passed.
- 2026-06-23: v233 operator dock compact responds to the user rejection that the v232 fixed dock became too large. `src/app.js` moves mobile operator page routes (`선수`, `기록`, `라이브`, `관리`) into `operator-mobile-top-route-v233` in the top command area and rebuilds the fixed dock as one row with the primary action plus `경기`/`설정`/`도구` only. `src/styles/app.css` restores the mobile command bar for that top route row, hides the desktop command/account groups on mobile, sets `operator-mobile-dock-v233` to one row, and reduces bottom clearance to 104px. `tools/verify-static.js` blocks `operator-mobile-route-v232` from returning to `src/app.js`, and `tools/qa-operator-flow.cjs` checks the top route clicks plus one-row dock height. Build metadata is now version 233, label BUILD v233 OPERATOR DOCK COMPACT, assets build/app/css 233. Validation passed: `node --check src/app.js`, `node --check src/core/build.js`, `node --check tools/verify-static.js`, `node --check tools/qa-operator-flow.cjs`, `npm.cmd run verify`, `tools/qa-operator-flow.cjs` with Codex `NODE_PATH`, `git diff --check`, and `git diff --cached --check`. GitHub Pages deployment passed after force push, and public index/build/app/css v233 asset checks passed.
- 2026-06-23: v232 operator nav access fixes the mobile operator screen having no path to other app screens after the top command bar was hidden on mobile. `src/app.js` adds `operator-mobile-route-v232` buttons for `선수`, `기록`, `라이브`, and `관리` inside the fixed mobile dock while keeping `경기`/`설정`/`도구`; `src/styles/app.css` adds the matching route row and increases mobile bottom clearance. `tools/verify-static.js` and `tools/qa-operator-flow.cjs` now require those mobile route controls and click them in mobile QA, including player list, dashboard, live popup, and admin. Build metadata is now version 232, label BUILD v232 OPERATOR NAV ACCESS, assets build/app/css 232. Validation passed: `node --check src/app.js`, `node --check src/core/build.js`, `node --check tools/verify-static.js`, `node --check tools/qa-operator-flow.cjs`, `npm.cmd run verify`, `tools/qa-operator-flow.cjs` with Codex `NODE_PATH`, `git diff --check`, and `git diff --cached --check`. GitHub Pages deployment passed after force push, and public index/build/app/css v232 asset checks passed.
- 2026-06-23: v231 UI copy cleanup responds to the user request to remove duplicate labels and system-facing expressions. `src/app.js` now removes visible SETUP/INFO/RULE/MODE/TOOLS/ACTION/QUEUE-style labels from the operator panels, changes player DB wording to player list wording, hides LIVE ID/automatic unique key/raw heartbeat/lease copy, shortens lower tool empty states, and localizes live lobby waiting/recent/empty badges. `tools/verify-static.js` and `tools/qa-operator-flow.cjs` now guard against those removed strings returning in operator UI. Build metadata is now version 231, label BUILD v231 UI COPY CLEANUP, assets build/app/css 231. Validation passed: `node --check src/app.js`, `node --check src/core/build.js`, `node --check tools/verify-static.js`, `node --check tools/qa-operator-flow.cjs`, `npm.cmd run verify`, `tools/qa-operator-flow.cjs` with Codex `NODE_PATH`, `git diff --check`, and `git diff --cached --check`. GitHub Pages deployment passed after force push, and public index/build/app/css v231 asset checks passed.
- 2026-06-23: v230 operator side panel unify continues the operator UI cleanup below the active match controller. `src/app.js` now uses `renderOperatorSubheadV230()` and `operator-unified-section-v230` across the lower `대회 준비` info/rule/mode panels and the `도구` panel, with `operator-tool-primary-row-v230` for the main manual action row. `src/styles/app.css` adds the matching v230 shared section/subhead/tool-row rules, and `tools/verify-static.js` plus `tools/qa-operator-flow.cjs` guard the new lower-panel structure. Build metadata is now version 230, label BUILD v230 OPERATOR SIDE UNIFY, assets build/app/css 230. Validation passed: `node --check src/app.js`, `node --check src/core/build.js`, `node --check tools/verify-static.js`, `node --check tools/qa-operator-flow.cjs`, `npm.cmd run verify`, `tools/qa-operator-flow.cjs` with Codex `NODE_PATH`, `git diff --check`, and `git diff --cached --check`. GitHub Pages deployment passed after force push, and public index/build/app/css v230 asset checks passed.
- 2026-06-23: v229 advance selection save fixes the operator report that pressing group racer advance left the screen unchanged. `toggleAdvance()` now records the selected/unselected state with `logTournamentAction("진출자 변경", ...)` and immediately calls `saveLiveState()`, putting group advance clicks on the same local/Firebase live-save path as point score input. `tools/qa-operator-flow.cjs` now has an `advance-selection` check that clicks a real group slot and verifies `advanceIds`, selected UI, and local persisted state. Build metadata is now version 229, label BUILD v229 ADVANCE SELECTION SAVE, assets build/app/css 229. Validation passed: `node --check src/app.js`, `node --check src/core/build.js`, `node --check tools/verify-static.js`, `node --check tools/qa-operator-flow.cjs`, `npm.cmd run verify`, `tools/qa-operator-flow.cjs` with Codex `NODE_PATH`, and `git diff --check`. GitHub Pages deployment passed after force push, and public index/build/app/css v229 asset checks passed.
- 2026-06-23: v228 finalist action feedback fixes the operator complaint that pressing the finalist action looked unresponsive. The visible controller now shows `결승 진출자 확정` only on confirmable final stages (`라운드 결승`, `결승`, or `pointFinal`), while `confirmRoundFinalist()` keeps the same guard internally. Operator feedback/error output moved directly under the controller with `operator-feedback-v228`, so invalid actions are visible near the clicked controls instead of below the whole task stack. `tools/verify-static.js` and `tools/qa-operator-flow.cjs` now guard the v228 marker, action visibility, and feedback placement. Build metadata is now version 228, label BUILD v228 FINALIST ACTION FEEDBACK, assets build/app/css 228. Validation passed: `node --check src/app.js`, `node --check src/core/build.js`, `node --check tools/verify-static.js`, `node --check tools/qa-operator-flow.cjs`, `npm.cmd run verify`, `npm.cmd run qa:audit`, `tools/qa-operator-flow.cjs`, `tools/qa-admin-flow.cjs`, `tools/qa-result-matrix.cjs`, `tools/qa-surface-check.cjs` with Codex `NODE_PATH`, and `git diff --check`. GitHub Pages deployment passed after force push, and public index/build/app/css v228 asset checks passed.
- 2026-06-23: v227 operator lower workflow continues the user-requested cleanup below the visible controller. `src/app.js` adds v227 structure classes across the operator task stack, current round, current stage board, stage/group/slot DOM, queue, final wrapper, setup panel, and tools panel while keeping the v226 controller and mobile fixed primary behavior. `src/styles/app.css` adds a scoped v227 lower-workflow block so current task, stage groups, queue, final wrapper, setup, and tools share the same compact frame. `tools/verify-static.js` and `tools/qa-operator-flow.cjs` now guard the v227 lower operator DOM. Build metadata is now version 227, label BUILD v227 OPERATOR LOWER WORKFLOW, assets build/app/css 227. Validation passed: `node --check src/app.js`, `node --check src/core/build.js`, `node --check tools/verify-static.js`, `node --check tools/qa-operator-flow.cjs`, `npm.cmd run verify`, `npm.cmd run qa:audit`, `tools/qa-operator-flow.cjs`, `tools/qa-admin-flow.cjs`, `tools/qa-result-matrix.cjs`, `tools/qa-surface-check.cjs` with Codex `NODE_PATH`, and `git diff --check`. GitHub Pages deployment passed after force push, and public index/build/app/css v227 asset checks passed.
- 2026-06-23: v226 operator frame alignment responds to the screenshot feedback that the operator screen had mixed card, tab, controller, and dock rules. `src/app.js` now renders a single v226 operator overview, `operator-round-rail-v226`, `renderOperatorControlConsoleV226()`, and `renderOperatorMobileDockV226()` while preserving tournament logic and Firebase behavior. The visible `OP` badge and mobile duplicate primary action were removed; mobile uses only the fixed dock primary while desktop keeps the controller primary. `src/styles/app.css` replaces the v225 OP/messenger selector block with scoped v226 frame alignment rules and removes the stale CSS-only `operator-round-tabs-v224` selector. `tools/qa-operator-flow.cjs` now checks the v226 controller, mobile dock primary, and absence of the OP badge. Build metadata is now version 226, label BUILD v226 OPERATOR FRAME ALIGNMENT, assets build/app/css 226. Validation passed: `node --check src/app.js`, `node --check src/core/build.js`, `node --check tools/verify-static.js`, `node --check tools/qa-operator-flow.cjs`, `npm.cmd run verify`, `npm.cmd run qa:audit`, `tools/qa-operator-flow.cjs`, `tools/qa-admin-flow.cjs`, `tools/qa-result-matrix.cjs`, `tools/qa-surface-check.cjs` with Codex `NODE_PATH`, and `git diff --check`. GitHub Pages deployment passed after force push, and public index/build/app/css v226 asset checks passed.
- 2026-06-23: v225 operator control console responds to the user report that the operator screen had lost an obvious controller and should feel simpler like a messenger app. `src/app.js` adds `renderOperatorControlConsoleV225()` above the active round, moving start/next/finalist/reset/group-size controls out of the round header into one visible controller while preserving tournament logic and Firebase behavior. `src/styles/app.css` adds the scoped v225 messenger-style controller rules and removes obsolete `round-actions-compact-v136` / `mobile-*-action-v147` CSS-only remnants. `tools/qa-operator-flow.cjs` now fails if the operator controller is missing. Build metadata is now version 225, label BUILD v225 OPERATOR CONTROL CONSOLE, assets build/app/css 225.
- 2026-06-23: Latest user instruction stops the split-chat structure and limits this implementation pass to v224 only. The later v225 Player DB workspace change was removed from the current snapshot by restoring the app/build/static verification files to the v224 operator UI foundation state; `TASK_QUEUE.md` now marks split-chat coordination as stopped and `IMPLEMENTATION_REPORT.md` records v224-only completion. Current public app target is version 224, label BUILD v224 OPERATOR UI FOUNDATION, assets build/app/css 224.
- 2026-06-23: v224 operator UI foundation starts the accepted full-app redesign from the v222 baseline without continuing the rejected v223 CSS-only direction. `src/app.js` now adds v224 common shell/workspace/status/panel helpers, rebuilds the operator DOM around current task, queue, setup, tools, and a mobile primary action plus three-item dock; `src/styles/app.css` adds matching v224 layout rules; `tools/verify-static.js` requires the v224 marker. Build metadata now reports version 224, label BUILD v224 OPERATOR UI FOUNDATION, assets build/app/css 224. Validation passed: `node --check src/app.js`, `node --check src/core/build.js`, `node --check tools/verify-static.js`, `npm.cmd run verify`, `npm.cmd run qa:audit`, `tools/qa-operator-flow.cjs`, `tools/qa-admin-flow.cjs`, `tools/qa-result-matrix.cjs`, `tools/qa-surface-check.cjs` with Codex `NODE_PATH`, and `git diff --check`; GitHub Pages deployed successfully and public index/build/app/css v224 asset checks passed.
- 2026-06-23: Multi-chat reporting now uses shared files so the master chat can inspect results without pasted logs: `DESIGN_OUTPUT.md`, `IMPLEMENTATION_REPORT.md`, `QA_REPORT.md`, and `FIREBASE_REPORT.md`. `TASK_QUEUE.md` assigns each role to its report file.
- 2026-06-23: Multi-chat UI redesign coordination files were added without app code changes: `UI_REDESIGN_BRIEF.md` is the shared full-app redesign brief, and `TASK_QUEUE.md` defines master/design/implementation/verification/Firebase chat roles plus the v224 starting instruction. App baseline remains v222, label BUILD v222 AUX SURFACE DESIGN.
- 2026-06-23: Rejected v223 luxury-minimal CSS-only attempt was rolled back after the user reported the player DB was broken and rejected the override-style redesign approach. Current public build metadata is restored to version 222, label BUILD v222 AUX SURFACE DESIGN, assets build/app/css 222; v223 markers were removed from index/build/CSS/static verification. Rollback validation passed locally: `node --check src/app.js`, `node --check src/core/build.js`, `node --check tools/verify-static.js`, `npm.cmd run verify`, `npm.cmd run qa:audit`, `tools/qa-admin-flow.cjs`, `tools/qa-result-matrix.cjs`, `tools/qa-operator-flow.cjs`, `tools/qa-surface-check.cjs`, and `git diff --check`.
- 2026-06-23: v222 auxiliary surface design extends the v218-v221 polish to remaining public/support screens without tournament logic changes. `src/styles/app.css` adds a v222 CSS-only layer for login/restricted cards, LIVE lobby cards/history, mobile LIVE current/history blocks, print screen controls/header, and dashboard error/auth fallback surfaces; `tools/verify-static.js` now requires the v222 marker. Build metadata now reports version 222, label BUILD v222 AUX SURFACE DESIGN, assets build/app/css 222. Validation passed: `node --check src/app.js`, `node --check src/core/build.js`, `node --check tools/verify-static.js`, `npm.cmd run verify`, `npm.cmd run qa:audit`, `tools/qa-surface-check.cjs`, `tools/qa-operator-flow.cjs`, `tools/qa-admin-flow.cjs`, `tools/qa-result-matrix.cjs`, and `git diff --check`. Deployed via GitHub Pages run 28001641979; public index/build/app/css v222 asset checks passed.
- 2026-06-23: v221 design system QA completes the requested v218-v221 visual polish sequence without tournament logic changes. `src/styles/app.css` adds v218 restrained design tokens/shared surface polish, v219 calmer mobile operator composition, v220 DB/admin operational density, and v221 CSS-only overflow/letter-spacing guardrails. `tools/verify-static.js` now requires those markers, and build metadata now reports version 221, label BUILD v221 DESIGN SYSTEM QA, assets build/app/css 221. Validation passed: `node --check src/app.js`, `node --check src/core/build.js`, `node --check tools/verify-static.js`, `npm.cmd run verify`, `npm.cmd run qa:audit`, `tools/qa-operator-flow.cjs`, `tools/qa-admin-flow.cjs`, `tools/qa-surface-check.cjs`, `tools/qa-result-matrix.cjs`, and `git diff --check`. Deployed via GitHub Pages run 28000878467; public index/build/app/css v221 asset checks passed.
- 2026-06-23: v217 CSS-only remnant cleanup removes additional unused helper selectors without runtime behavior changes. `src/styles/app.css` drops `page-link`, `section-label`, `firebase-autosave`, `needs-contact`, `identity-note`, and `current-page-action`; `tools/verify-static.js` now blocks those classes from returning. Build metadata now reports version 217, label BUILD v217 CSS ONLY REMNANT CLEANUP, assets build/app/css 217. Validation passed: `node --check src/app.js`, `node --check src/core/build.js`, `node --check tools/verify-static.js`, `npm.cmd run verify`, `npm.cmd run qa:audit`, `tools/qa-operator-flow.cjs`, `tools/qa-admin-flow.cjs`, `tools/qa-surface-check.cjs`, `tools/qa-result-matrix.cjs`, and `git diff --check`. Deployed via GitHub Pages run 27999117987; public index/build/app/css v217 asset checks passed.
- 2026-06-23: v216 legacy CSS selector trim removes additional dead CSS remnants without runtime behavior changes. `src/styles/app.css` drops retired mobile/TV helper selectors including `mobile-operator-layout`, `mobile-section-stack`, `mobile-section-card`, `quick-step-panel`, `slot-player-line`, `team-inline`, `tv-team-inline`, `tv-h2h-inline`, and `tv-lane-no-main`; `tools/verify-static.js` now blocks those classes from returning. Build metadata now reports version 216, label BUILD v216 LEGACY CSS SELECTOR TRIM, assets build/app/css 216. Validation passed: `node --check src/app.js`, `node --check src/core/build.js`, `node --check tools/verify-static.js`, `npm.cmd run verify`, `npm.cmd run qa:audit`, `tools/qa-operator-flow.cjs`, `tools/qa-admin-flow.cjs`, `tools/qa-surface-check.cjs`, `tools/qa-result-matrix.cjs`, and `git diff --check`. Deployed via GitHub Pages run 27998539017; public index/build/app/css v216 asset checks passed.
- 2026-06-23: v215 CSS dead selector cleanup continues the mobile operator declutter work without changing runtime behavior. `src/styles/app.css` removes dead `history-list-v122 > .hint` styling plus retired mobile operator titlebar `.desc` selectors, and `tools/qa-operator-flow.cjs` drops the already-removed `prep-helper-v115` clutter selector. Build metadata now reports version 215, label BUILD v215 CSS DEAD SELECTOR CLEANUP, assets build/app/css 215. Validation passed: `node --check src/core/build.js`, `node --check tools/qa-operator-flow.cjs`, `npm.cmd run verify`, `npm.cmd run qa:audit`, `tools/qa-operator-flow.cjs`, `tools/qa-admin-flow.cjs`, `tools/qa-surface-check.cjs`, `tools/qa-result-matrix.cjs`, and `git diff --check`. Deployed via GitHub Pages run 27997953749; public index/build/app/css v215 asset checks passed.
- 2026-06-23: v214 mobile operator declutter simplifies the mobile operator workflow after the user reported visual complexity. src/app.js now hides the operator header description on mobile, keeps the setup panel open only before an active stage exists, routes the bottom dock to current match / settings / tools / player DB, removes repeated prep/mode/history/lane-label helper copy, shortens final/LIVE/operator-stage guidance, and reduces stage metadata to operational essentials. src/styles/app.css adds the scoped v214 mobile operator panel/meta styling and removes dead prep-helper-v115 CSS. tools/qa-operator-flow.cjs now excludes closed details content from visible-copy counting and fails if mobile current-stage panels stay open or visible helper copy grows again. Build metadata now reports version 214, label BUILD v214 MOBILE OPERATOR DECLUTTER, assets build/app/css 214. Validation passed: node --check src/app.js, node --check src/core/build.js, node --check tools/qa-operator-flow.cjs, npm.cmd run verify, npm.cmd run qa:audit, tools/qa-operator-flow.cjs, tools/qa-admin-flow.cjs, tools/qa-surface-check.cjs, tools/qa-result-matrix.cjs, and git diff --check. Deployed via GitHub Pages run 27997383560; public index/build/app/css v214 asset checks passed.
- 2026-06-19: v213 mobile slot readability fixes the operator mobile current-stage cards shown in the user screenshot. `src/styles/app.css` adds a final scoped v213 mobile-operator patch so slot lane labels no longer clip, player name/team and recent head-to-head records keep separate readable columns, group spacing is calmer, and the fixed bottom operator dock has enough scroll clearance. `tools/qa-operator-flow.cjs` now measures mobile operator slot width/height, head-to-head rail width, lane clipping, and dock clearance so this regression is caught. Build metadata now reports version 213, label BUILD v213 MOBILE SLOT READABILITY, assets build/app/css 213. Validation passed: `node --check src/core/build.js`, `node --check tools/qa-operator-flow.cjs`, `npm.cmd run verify`, `npm.cmd run qa:audit`, `tools/qa-operator-flow.cjs`, `tools/qa-admin-flow.cjs`, `tools/qa-surface-check.cjs`, `tools/qa-result-matrix.cjs`, and `git diff --check`. Deployed via GitHub Pages run 27807924534; public index/build/app/css v213 asset checks passed.
- 2026-06-18: v212 surface UI spine continues the accepted one-screen-at-a-time unification pass beyond operator v211. `src/app.js` extends shared command bars and shell/panel classes to admin, player DB, dashboard, LIVE lobby, login, pending, restricted, and loading/error surfaces while preserving tournament logic and existing QA hook classes. `src/styles/app.css` adds the scoped v212 shared command bar, workspace, panel, auth, admin, DB, dashboard, and live-lobby rules; the DB command bar includes measured desktop/mobile height guards. Build metadata now reports version 212, label BUILD v212 SURFACE UI SPINE, assets build/app/css 212. Local validation passed: `node --check src/app.js`, `node --check src/core/build.js`, `npm.cmd run verify`, `npm.cmd run qa:audit`, `tools/qa-operator-flow.cjs`, `tools/qa-admin-flow.cjs`, `tools/qa-surface-check.cjs`, `tools/qa-result-matrix.cjs`, and `git diff --check`. Deployed via GitHub Pages run 27737912991; public index/build/app/css v212 asset checks passed.
- 2026-06-18: v211 operator UI spine starts the one-screen-at-a-time UI unification pass after the discarded prior v211 attempt. `src/app.js` adds `renderUiActionGroupV211()` and `renderOperatorCommandBarV211()`, moves the operator navigation/tools/account controls into a shared `ui-actionbar-v211`, and marks the operator shell/workspace/main/side panels with v211 classes without changing tournament logic or Firebase behavior. `src/styles/app.css` adds a scoped v211 operator UI spine for desktop/mobile action bar and workspace layout. Build metadata now reports version 211, label BUILD v211 OPERATOR UI SPINE, assets build/app/css 211. Validation passed: `node --check src/app.js`, `node --check src/core/build.js`, `npm.cmd run verify`, `npm.cmd run qa:audit`, `tools/qa-operator-flow.cjs`, `tools/qa-surface-check.cjs`, `tools/qa-admin-flow.cjs`, `tools/qa-result-matrix.cjs` with Codex runtime NODE_PATH, `git diff --check`, and Browser local checks on stubbed operator desktop/mobile confirming v211 action bar, no horizontal overflow, and mobile dock intact. Deployed via GitHub Pages run 27729268544; public index/build/app/css v211 asset checks passed.
- 2026-06-16: v210 code/CSS audit adds `tools/audit-code-css.cjs` and `npm run qa:audit` to scan build metadata, asset query strings, CSS-only versioned classes, duplicate/long/broad selectors, `!important` volume, inline handlers, and runtime wrapper layers. The audit now fails on CSS-only versioned classes. Removed dead CSS selectors for `admin-toolbar-spacer-v145` and `admin-venue-chip-v186`, and `tools/verify-static.js` blocks their return. Build metadata now reports version 210, label BUILD v210 CODE CSS AUDIT, assets build/app/css 210. Validation passed: `npm.cmd run verify`, `npm.cmd run qa:audit`, `node --check src/core/build.js`, `node --check tools/audit-code-css.cjs`, `node --check tools/verify-static.js`, `git diff --check`, `tools/qa-admin-flow.cjs`, `tools/qa-operator-flow.cjs`, `tools/qa-result-matrix.cjs`, and `tools/qa-surface-check.cjs` with NODE_PATH. In-app Browser local check could not connect to the temporary local server in this environment; Playwright local QA covered the representative screens.
- 2026-06-15: v209 admin toggle size unifies the clickable permission chips in the admin account list. `permission-flags-v204` now uses fixed grid tracks so the four permission toggles stay the same size on desktop and in the two-column mobile layout, and `tools/qa-admin-flow.cjs` now fails if any account-row toggle widths/heights diverge. Build metadata now reports version 209, label BUILD v209 ADMIN TOGGLE SIZE, assets build/app/css 209. Validation passed: `npm.cmd run verify`, `git diff --check`, `node --check src/core/build.js`, `node --check tools/qa-admin-flow.cjs`, `tools/qa-admin-flow.cjs`, `tools/qa-operator-flow.cjs`, `tools/qa-surface-check.cjs`, `tools/qa-result-matrix.cjs` with NODE_PATH, and Browser local admin checks at `http://127.0.0.1:8765/#view=admin` confirming equal-sized mobile permission chips.
- 2026-06-15: v208 dashboard account cleanup finishes the dashboard top account card so it matches the newer admin toolbar pattern without keeping the old dashboard-specific toolbar layer alive. The dashboard now renders only `dashboard-account-strip-v207` from `accountBadge()`, the retired `renderDashboardToolbarV146` function and every `dashboard-toolbar-v146` / `toolbar-spacer-v146` CSS selector were removed, and `tools/verify-static.js` now fails if that legacy dashboard toolbar layer comes back. Build metadata now reports version 208, label BUILD v208 DASHBOARD ACCOUNT CLEANUP, assets build/app/css 208. Validation passed: `npm.cmd run verify`, `node --check src/app.js`, `node --check src/core/build.js`, `node --check tools/qa-operator-flow.cjs`, `node --check tools/verify-static.js`, `git diff --check`, `tools/qa-operator-flow.cjs`, `tools/qa-admin-flow.cjs`, `tools/qa-surface-check.cjs`, `tools/qa-result-matrix.cjs` with NODE_PATH, and Browser local dashboard checks at `http://127.0.0.1:8765/#view=dashboard` for desktop plus 390px mobile showing build 208 and the unified account strip.
- 2026-06-15: v206 admin toolbar fill fixes the visible mobile admin toolbar issue left after v205. The admin toolbar button stack now spans the full card width on admin mobile instead of sitting in only one half of the parent grid, while keeping the three button rows and the account pill at the bottom. `tools/qa-admin-flow.cjs` now measures toolbar width vs button-stack width and fails if the mobile button block no longer fills the toolbar card. Build metadata now reports version 206, label BUILD v206 ADMIN TOOLBAR FILL, assets build/app/css 206. Validation passed: `npm.cmd run verify`, `git diff --check`, `node --check src/core/build.js`, `node --check tools/qa-admin-flow.cjs`, `tools/qa-admin-flow.cjs`, `tools/qa-operator-flow.cjs`, `tools/qa-surface-check.cjs`, and `tools/qa-result-matrix.cjs` with NODE_PATH.
- 2026-06-15: v205 admin account align moves the admin account table to three columns (`계정`, `상태`, `권한`), groups venue name above the account email in the first column, and keeps the permission toggles in the far-right cell without restoring a separate management column. Desktop permission chips stay auto-sized on one row, while mobile permission chips use a compact two-column layout so the list still fits at 390px and 320px without horizontal scroll. `tools/qa-admin-flow.cjs` now checks the new header order and requires one venue line per account row. Build metadata now reports version 205, label BUILD v205 ADMIN ACCOUNT ALIGN, assets build/app/css 205. Validation passed: `npm.cmd run verify`, `git diff --check`, `node --check src/app.js`, `node --check tools/qa-admin-flow.cjs`, `node --check tools/verify-static.js`, `tools/qa-admin-flow.cjs`, `tools/qa-operator-flow.cjs`, `tools/qa-surface-check.cjs`, and `tools/qa-result-matrix.cjs` with NODE_PATH.
- 2026-06-15: v204 admin permission fit merges admin account row management into the current-permission cell. The admin toolbar now renders as three button rows with a distinct logout button and the account pill at the bottom. Admin account rows removed the separate management column; 운영/대시보드/관리자/중지 are clickable `permission-toggle-v204` chips, and mobile account tables fit without horizontal scroll at 390px and 320px. The common toolbar flex rules now exclude `admin-unified-toolbar-v145` so the admin toolbar is not re-flattened by older shared CSS. Build metadata now reports version 204, label BUILD v204 ADMIN PERMISSION FIT, assets build/app/css 204. Validation passed: npm.cmd run verify, tools/qa-admin-flow.cjs, tools/qa-operator-flow.cjs, tools/qa-surface-check.cjs, tools/qa-result-matrix.cjs with NODE_PATH, node --check for changed JS QA/static files, and git diff --check. In-app Browser local port check still could not connect in this environment; Playwright QA covered the admin PC/390/320 layouts.
- 2026-06-15: v203 DB roster fit fixes the player DB roster table density after the toolbar cleanup. The roster table now carries an admin/venue class so column widths are calculated separately, desktop roster wrappers no longer create horizontal scroll, row inputs are 28px high, and favorite star buttons are excluded from broad global button sizing so they stay 24px inside their cells. `tools/qa-operator-flow.cjs` now uses an admin fake user plus seeded roster rows and fails on desktop roster horizontal scroll, tall rows, oversized stars, or star cell overlap. Build metadata now reports version 203, label BUILD v203 DB ROSTER FIT, assets build/app/css 203. Validation passed: npm.cmd run verify, tools/qa-operator-flow.cjs, tools/qa-admin-flow.cjs, tools/qa-surface-check.cjs, tools/qa-result-matrix.cjs with NODE_PATH, and git diff --check.
- 2026-06-15: v202 DB toolbar groups cleans up the player DB top command row by grouping navigation, file actions, and account state; labels were shortened (`뒤로`, `CSV`, `양식`, `업로드`), desktop stays one compact row, and mobile hides the email pill while keeping the command bar under the new QA height guard. The CSS change was consolidated into the existing player-management toolbar area and broad common button/label rules now exclude this command bar instead of requiring another late override block. Build metadata now reports version 202, label BUILD v202 DB TOOLBAR GROUPS, assets build/app/css 202. Validation passed: npm.cmd run verify, tools/qa-operator-flow.cjs, tools/qa-admin-flow.cjs, tools/qa-surface-check.cjs, tools/qa-result-matrix.cjs with NODE_PATH, and git diff --check.
- 2026-06-15: v201 DB add button density keeps the mobile player DB primary actions compact inside the existing v191 mobile table layer. The `선수 추가` and selected-player primary buttons are now 32px high on mobile with reduced top margin, and `tools/qa-operator-flow.cjs` fails if the mobile DB add-player button grows above 34px. Build metadata now reports version 201, label BUILD v201 DB ADD BUTTON DENSITY, assets build/app/css 201. Validation passed: npm.cmd run verify, node tools/qa-result-matrix.cjs with NODE_PATH, tools/qa-admin-flow.cjs, tools/qa-operator-flow.cjs, tools/qa-surface-check.cjs, and git diff --check.
- 2026-06-15: v200 header top alignment removes stacked top spacing from shared non-TV surfaces. The common surface shell now uses a fixed operator-like 14px desktop top rhythm and 8px mobile top rhythm; dashboard, admin, player DB, and LIVE lobby page-shell top margins were changed to 0 so they no longer add a second 14px layer above the header. QA now records/fails large header top gaps in operator/dashboard/DB, admin, and LIVE lobby checks. Build metadata now reports version 200, label BUILD v200 HEADER TOP ALIGN, assets build/app/css 200. Validation passed: npm.cmd run verify, npm.cmd run qa:result with NODE_PATH, tools/qa-admin-flow.cjs, tools/qa-operator-flow.cjs, tools/qa-surface-check.cjs, and git diff --check.
- 2026-06-15: v199 admin match row removes the visible private/public/local source badge from the admin tournament-record table, keeps only the tournament name in the first column, and renames the source summary to `저장 기록` so `비공개` is no longer shown as a row label. Match record rows now keep cells single-line with ellipsis, and `tools/qa-admin-flow.cjs` fails if record subtext/source badges return. `tools/qa-surface-check.cjs` now waits for LIVE lobby cards before inspecting that route. Build metadata now reports version 199, label BUILD v199 ADMIN MATCH ROW, assets build/app/css 199. Validation passed: npm.cmd run verify, npm.cmd run qa:result with NODE_PATH, tools/qa-admin-flow.cjs, tools/qa-operator-flow.cjs, tools/qa-surface-check.cjs, and git diff --check.
- 2026-06-15: v198 admin account row removes redundant account subtext plus permission badge/detail copy from the admin account table, leaving only email and permission ON/OFF chips. The management column is widened and `admin-actions-v186` is four columns so 운영/대시보드/관리자/중지 actions stay on one row. `tools/qa-admin-flow.cjs` now fails if the removed account/permission copy returns or if account action buttons wrap. Build metadata now reports version 198, label BUILD v198 ADMIN ACCOUNT ROW, assets build/app/css 198. Validation passed: npm.cmd run verify, npm.cmd run qa:result with NODE_PATH, tools/qa-admin-flow.cjs, tools/qa-operator-flow.cjs, tools/qa-surface-check.cjs, and git diff --check.
- 2026-06-15: v197 session lease layout fixes the operator `다중접속 · 새로고침 보호` panel by changing the existing v178 session lease header to a single-column header: text/status/heartbeat stay full width, and the three action buttons sit on their own row. This prevents the left copy from collapsing into vertical one-character text. `tools/qa-operator-flow.cjs` now opens the operator ops section and fails if the lease title is squeezed or the actions overlap the title row. Build metadata now reports version 197, label BUILD v197 SESSION LEASE LAYOUT, assets build/app/css 197.
- 2026-06-15: v196 prep action flatten removes the obsolete `prep-preview-v116`/`prep-summary-v143` wrapper around tournament status action buttons so the finished-state `새 대회 준비` / `종료 취소` controls sit directly in the prep card without a second inner layer. Related CSS blocks were removed instead of adding another override, and `tools/verify-static.js` now blocks reintroducing those wrapper classes. Build metadata now reports version 196, label BUILD v196 PREP ACTION FLATTEN, assets build/app/css 196.
- 2026-06-15: v195 unused CSS cleanup removes obsolete versioned CSS-only blocks left behind by older UI iterations: unused live lobby v88/v89 selectors, prep v115/v116 selectors, DB action/batch v119/v121 selectors, DB top v131 selectors, old group-size v133/v136/v138/v143 controls, prep summary copy v143, and mobile point score v145. `tools/verify-static.js` now fails if those classes are reintroduced. Build metadata now reports version 195, label BUILD v195 UNUSED CSS CLEANUP, assets build/css 195 and app 192. Validation passed: npm.cmd run verify, npm.cmd run qa:result with NODE_PATH, tools/qa-admin-flow.cjs, tools/qa-operator-flow.cjs, tools/qa-surface-check.cjs, git diff --check, GitHub Pages run 27522510647, public v195 asset fetch checks, and Browser public LIVE lobby DOM check.
- 2026-06-15: v194 admin CSS consolidation removes the old v177 mobile admin card/table conversion blocks and folds the v193 compact admin row/action density directly into the v191 mobile table layer. `tools/verify-static.js` now fails if the old admin mobile card conversion or a new v193 override layer is reintroduced. Build metadata now reports version 194, label BUILD v194 ADMIN CSS CONSOLIDATION, assets build/css 194 and app 192. Validation passed: npm.cmd run verify, npm.cmd run qa:result with NODE_PATH, tools/qa-admin-flow.cjs, tools/qa-operator-flow.cjs, tools/qa-surface-check.cjs, and git diff --check. In-app Browser could not reach the local 127.0.0.1 server in this environment; Playwright QA verified the mobile admin account rows at 33px.
- 2026-06-15: v193 admin mobile density tightens admin account and tournament-record mobile list rows. Admin account mobile rows now keep the action buttons in one compact row, widen the horizontal table for the action column, hide redundant secondary text in mobile table cells, and lower QA mobile admin account row height from 72px to 33px. Build metadata now reports version 193, label BUILD v193 ADMIN MOBILE DENSITY, assets build/css 193 and app 192. Validation passed: npm.cmd run verify, npm.cmd run qa:result with NODE_PATH, tools/qa-admin-flow.cjs, tools/qa-operator-flow.cjs, tools/qa-surface-check.cjs, and git diff --check.
- 2026-06-15: v192 dashboard win count stops counting plain `진출` rows as wins in dashboard and metric summaries. Countable win rows are now rank-1 results, `결승진출`, `최종결승진출`, `최종우승`, plus point heat winners; generic advance rows still remain in result history but no longer inflate `최다승` or lane wins. Added `runPlainAdvanceDoesNotCountCase` to `tools/qa-result-matrix.cjs` and a static guard in `tools/verify-static.js`. Build metadata now reports version 192, label BUILD v192 DASHBOARD WIN COUNT, assets build/app 192 and css 191. Validation passed: npm.cmd run verify, npm.cmd run qa:result with NODE_PATH, tools/qa-admin-flow.cjs, tools/qa-operator-flow.cjs, tools/qa-surface-check.cjs, and git diff --check.
- 2026-06-15: v191 compact DB/admin mobile cards changes the player DB and admin list mobile layout from tall stacked row cards to compact horizontal tables inside scroll containers. Player DB mobile selected/bulk controls, stat chips, filters, roster rows, and admin overview/list rows were tightened; admin mobile account rows hide secondary permission detail chips while keeping permission status/actions visible. Build metadata now reports version 191, label BUILD v191 COMPACT DB ADMIN CARDS, assets build 191, app 190, css 191. Validation passed: npm.cmd run verify, git diff --check, tools/qa-admin-flow.cjs with NODE_PATH, tools/qa-operator-flow.cjs with NODE_PATH, and tools/qa-surface-check.cjs with NODE_PATH.
- 2026-06-15: v190 remote result priority fixes stale dashboard records after direct Firebase correction for tournament-20260613-2138. Firebase privateResultLogs venue record and publicHistory/tournament-20260613-2138 rows 49,67,79,86 were changed from final-qualifier to advance after backup exports to a temp folder outside the repo, correcting old non-final-stage final qualifier rows for two racers including Park Taejin. App dashboard merge order now prefers privateResultLogs, then publicHistory, then local logs so stale localStorage cannot shadow Firebase corrections. Build metadata now reports version 190, label BUILD v190 REMOTE RESULT PRIORITY, assets build/app 190 and css 186. Validation passed: npm.cmd run verify, npm.cmd run qa:result with NODE_PATH, git diff --check, tools/qa-admin-flow.cjs, tools/qa-operator-flow.cjs, and tools/qa-surface-check.cjs with NODE_PATH (surface passed on rerun after a transient live-lobby timing miss).
- 2026-06-15: v189 legacy point win recount expands dashboard and metric low-score detection for old points5Tree records that lack saved mode/비고 markers, using 경기방식, 포인트 5차전, 트리타기, and tie-break stage clues. Legacy 동점 순위 결정전 rows saved only as 진출 no longer all count as wins. Added tools/qa-result-matrix.cjs coverage for unmarked legacy points5 records. Build metadata now reports version 189, label BUILD v189 LEGACY POINT WIN RECOUNT, assets build/app 189 and css 186. Validation passed: npm.cmd run verify, npm.cmd run qa:result with NODE_PATH, git diff --check, tools/qa-operator-flow.cjs, tools/qa-admin-flow.cjs, tools/qa-surface-check.cjs with NODE_PATH, GitHub Pages run 27514940581, public v189 asset fetch checks, and Browser public DOM check.
- 2026-06-15: v188 result matrix QA adds tools/qa-result-matrix.cjs and npm script qa:result to exercise basic, points3, points5Tree, revival, crow, crow-semi, and legacy low-score records through the real browser getStageResultRows() and analyzeRecords() paths. Logic fixes: points5Tree dashboard/metric point wins now use low-score winners, point tie-break rows record rank results instead of marking every ordered player as advanced, rank-1 result rows count as wins, and revival final winners save as final champions instead of only final qualifiers. Build metadata now reports version 188, label BUILD v188 RESULT MATRIX QA, assets build/app 188 and css 186. Local validation passed: npm.cmd run verify, npm.cmd run qa:result with NODE_PATH, git diff --check, tools/qa-operator-flow.cjs, tools/qa-admin-flow.cjs, and tools/qa-surface-check.cjs with NODE_PATH.
- 2026-06-15: v187 finalist row fix changes getStageResultRows() so a round finalist is recorded as 최종결승진출 only on the actual final-stage row (라운드 결승/결승/pointFinal), while earlier same-round appearances remain 진출. Build metadata now reports version 187, label BUILD v187 FINALIST ROW FIX, assets build/app 187 and css 186. Added tools/qa-operator-flow.cjs regression coverage for 박태진-style 3차 rows [진출, 진출, 최종결승진출]. Validation passed: npm.cmd run verify, git diff --check, tools/qa-operator-flow.cjs with NODE_PATH, tools/qa-admin-flow.cjs with NODE_PATH, and tools/qa-surface-check.cjs with NODE_PATH (surface QA passed on standalone rerun after an initial live-data timing miss).
- 2026-06-15: v186 admin command center pass reduces admin-page clutter by adding account/record overview rails, clearer status/source chips, denser but calmer PC tables, mobile-safe admin cards, and a Firebase-stubbed tools/qa-admin-flow.cjs browser QA. Build metadata now reports version 186, label BUILD v186 ADMIN COMMAND CENTER, assets build/app/css 186. Validation passed: npm.cmd run verify, git diff --check, tools/qa-admin-flow.cjs with NODE_PATH, tools/qa-surface-check.cjs with NODE_PATH, tools/qa-operator-flow.cjs with NODE_PATH, GitHub Pages deploy, and public v186 asset fetch checks.
- 2026-06-13: added repository `AGENT_MEMORY.md` and local `project-memory-keeper` skill to preserve key project facts across context compaction.
- 2026-06-13: v164 database optimization sanitizes `publicLive` state writes, bounds public history/live lobby reads, prunes future `actionLogs`, adds RTDB indexes, and adds `tools/database-maintenance.cjs` for export audit/patch planning.
- 2026-06-13: Firebase RTDB rules were deployed for v164 indexes; live DB maintenance patch compacted `publicLive` and capped old `actionLogs`, with a temp backup kept outside the repo.
- 2026-06-13: v165 UI alignment removes the nested `.mobile-history` scroll on mobile LIVE so the page scrolls naturally.
- 2026-06-13: v166 typography harmonization adds a non-TV role-based type scale for page titles, section titles, controls, metadata, badges, and lobby cards.
- 2026-06-13: v167 mobile operator compact pass makes round tabs two-line, reduces round/prep action button bulk, hides the empty round text card before stages exist, and flattens the tournament-start block.
- 2026-06-13: v168 mobile operator density pass removes mobile round-tab horizontal scrolling, shows only finalist nicknames in round tab/badge status, folds tournament meta into the top mobile overview, and tightens next-group-size spacing.
- 2026-06-13: v169 mobile operator overview dedupe hides the PC overview on mobile so only the compact mobile overview remains above the final/round sections.
- 2026-06-13: v170 live sync latency pass reduces queued Firebase live sync delay from 1400ms to 220ms, enforces a 320ms minimum write interval, and adds trailing sync so TV/mobile viewers receive the latest bracket state after rapid operator changes.
- 2026-06-13: v171 live route refresh pass re-runs the app router on hash changes so same-tab transitions into TV/mobile LIVE do not leave the operator DOM behind.
- 2026-06-13: v172 mobile prep CTA cleanup removes stray wrapper backgrounds/shadows behind the tournament start button and bumps the CSS asset cache.
- 2026-06-13: v173 mobile surface unification adds a shared page header for operator, login, player DB, dashboard, LIVE lobby, and mobile LIVE; standardizes visible Korean navigation labels; and adds CSS guardrails for mobile header height, stats, and toolbar grids.
- 2026-06-13: v174 player DB header fit correction restores auto-height unified titlebar behavior where older DB-specific titlebar CSS clipped the header on mobile and PC.
- 2026-06-13: v175 live result visibility shows mobile point-match score/winner/advance badges, keeps full TV nicknames in the 90-degree live layout, and tightens the TV recent-record column.
- 2026-06-13: v176 UI foundation pass adds non-TV design tokens, shared titlebar/toolbar/button/card/badge/input/stat styling, and `docs/ui-quality-v176.md` as the next UI QA checklist.
- 2026-06-14: v177 admin mobile list polish converts admin account/venue and tournament record lists into mobile card rows, adds unified admin header stats, preserves desktop tables, and documents the pass in `docs/ui-quality-v177.md`.
- 2026-06-14: v178 operator session lease adds per-tab `sessionId`, `operationLocks/leases/{venueId}` write ownership, `operationLocks/sessions/{venueId}/{uid}/{sessionId}` heartbeat, running-action lease guards, and refresh recovery candidates from `activeTournaments` plus `tournaments/{id}/state`; no Firebase rules change was required because `operationLocks` already exists.
- 2026-06-14: v179 legacy patch cleanup removes obsolete runtime self-audit hooks (`runV77CleanupAudit`, `runCommercialSelfAuditV58`, `runProductionAuditV85`, `runFinalPracticeAuditV95`, `runV102HotfixAudit`, `runMini4wdAuditsV152`) and the dead v102 action wrapper while keeping active live-sync/save/backup/session safeguards; `tools/verify-static.js` now blocks reintroduction and duplicate function declarations.
- 2026-06-14: v180 button alignment pass normalizes vertical centering for non-TV shared button groups, upload labels, tab buttons, direct text wrappers, operator mobile dock, group-size buttons, point buttons, admin actions, DB bulk buttons, and dashboard period buttons; TV LIVE remains untouched.
- 2026-06-14: v181 legacy runtime trim removes the superseded v102 mobile/TV live-sync runtime wrapper from `src/app.js`; base `watchFirebaseState()` still watches both `publicLive/{id}/state` and `publicLive/{id}`, while v104/v170 save batching, v135 backup/auto-close, and v178 session lease remain active. `tools/verify-static.js` blocks reintroduction of the removed v102 runtime names.
- 2026-06-14: v182 checkbox size fix adds `settings-checkline-v182` for the operator settings exclude-finalists checkbox and a non-TV global checkbox sizing guard so shared input min-height rules cannot inflate checkboxes into large square controls.
- 2026-06-14: v183 mobile ops dock replaces the mobile operator bottom-bar `LIVE` shortcut with `운영`, opens the `operatorOpsAreaV183` details section, and scrolls to operation stability/lock/save controls. `tools/verify-static.js` blocks reintroducing the old dock LIVE shortcut.
- 2026-06-14: v184 live route polish clears stale public LIVE fallback timers/listeners before viewer route changes, guards LIVE callbacks by current hash and tournament id, restores player DB bulk buttons to usable 44px mobile/40px PC controls, lets mobile unified header descriptions wrap instead of clipping, and fixes narrow mobile operator download buttons. Validation: `npm run verify` with Node PATH, direct `node tools/verify-static.js`, `git diff --check`, and browser checks for same-tab mobile LIVE -> TV LIVE, DB bulk buttons, narrow login/dashboard/admin/LIVE lobby headers, and narrow operator downloads.
- 2026-06-14: v185 runtime cleanup removes unused manual LIVE/share helpers, the old snapshot payload encoder, retired TV/mobile read-only renderers, unused DB/Firebase import helpers, and the historical release marker chain. The app now exposes a single `__mini4wdCurrentRelease` marker plus DOM release attributes, and `tools/verify-static.js` blocks reintroducing the removed runtime and marker names. Validation: direct `node tools/verify-static.js`, `npm run verify`, `git diff --check`, and browser checks for login, LIVE lobby, same-tab stale TV fallback, mobile LIVE lobby, and mobile DB guard.
- 2026-06-15: added type-based browser QA tooling without changing public build assets. `tools/qa-surface-check.cjs` covers login/auth guards, LIVE lobby aliases, mobile/TV missing-id behavior, missing TV record fallback, same-tab stale TV fallback, console errors, text encoding, and horizontal overflow across PC/390/320 viewports. `tools/qa-operator-flow.cjs` stubs Firebase/XLSX locally and checks fake-auth operator load, points5Tree mode, round start, point score clicks, ops section opening, DB route, and dashboard route across PC/390/320 viewports. Validation passed: direct `node tools/verify-static.js`, `npm run verify`, `git diff --check`, `tools/qa-surface-check.cjs`, and `tools/qa-operator-flow.cjs`.
- v161: completed surface-specific UI coverage for the main screens.
- v162: added build metadata and quality checks for the current cleanup baseline.
- v163: removed obsolete patch layers, legacy duplicate `boot()`, old v123 wrapper, v87/v88 LIVE lobby CSS, and tracked patch ZIP noise.
- v163: strengthened `tools/verify-static.js` to catch reintroduced legacy patch patterns.

## Known traps
- Context compaction can make previous deployment, commit, or Firebase facts stale. Verify volatile facts live.
- Do not reintroduce legacy `function boot()`, `installV123DeviceSplitPatch`, v87/v88 LIVE lobby CSS, or tracked `mini4wd_v*_patch.zip` files.
- Public viewer routes should not be overwritten by authentication callbacks.
- Some browser automation contexts may not expose app globals; prefer DOM build attributes and body/page classes for verification.
- GitHub Actions Node 20 warnings are non-blocking for now, but should be upgraded later.
- Firebase CLI is not currently available on PATH in this workspace; use export-based DB maintenance tooling unless live credentials/CLI are reconnected.

## Next cleanup targets
- Gradually migrate inline `onclick` and `onchange` handlers to centralized event binding.
- Continue splitting large `src/app.js` responsibilities into small modules only where it reduces real complexity.
- Keep CSS cleanup tied to representative surface checks so visual regressions do not slip in.
- Use `docs/ui-quality-v176.md` as the baseline checklist for the next UI/detail pass.
- Use `docs/ui-quality-v177.md` when checking admin account/venue and tournament record list regressions.
- Use `docs/operator-session-recovery-v178.md` when checking same-account multi-tab, browser refresh, or remote restore behavior.
- Use `docs/cleanup-v179.md` when checking whether old runtime audit hooks or dead wrapper-style patches should stay removed.
- Use `docs/button-alignment-v180.md` when checking button vertical alignment regressions across mobile/PC non-TV surfaces.
- Use `docs/cleanup-v181.md` when checking why the old v102 live-sync runtime wrapper should stay removed.
- Add more focused regression tests around tournament ladder/tie-break flows when that logic is touched again.
