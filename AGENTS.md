# Repository Working Agreement

이 파일은 저장소 전체에 적용되는 영구 작업 규칙이다. 버전, 커밋, 배포 실행 번호처럼 바뀌는 사실은 AGENT_MEMORY.md에서 확인하고, 충돌할 때는 항상 현재 코드와 Git 상태를 우선한다.

## 신뢰 순서

1. 사용자의 가장 최근 요청과 명시적 승인
2. 현재 작업 트리, Git 원격, 빌드 메타데이터, 공개 사이트, 필요한 경우의 Firebase 실상
3. 이 파일의 영구 작업 규칙
4. AGENT_MEMORY.md의 현재 상태와 인수인계 정보
5. 그 밖의 보고서와 과거 문서

과거 버전 메모나 다른 작업의 설명이 현재 코드와 다르면 현재 증거를 따른다. 추측으로 오래된 지침을 되살리지 않는다.

## 권한과 작업 범위

- 설명, 진단, 검토 요청은 읽기 전용으로 처리한다. 사용자가 수정, 작업, 적용, 구현을 요청했을 때만 파일을 바꾼다.
- 명시적으로 요청된 수정은 해당 범위의 구현, 필요한 테스트, 커밋, main 반영, GitHub Pages 확인까지 완료하는 것을 기본으로 한다.
- Firebase 규칙 또는 운영 데이터 쓰기, 계정과 권한 변경, 대량 삭제, 자동화 생성·재개·변경은 별도의 명시적 요청이 필요하다.
- 실제 장애가 Firebase 상태와 관련될 때는 필요한 최소 범위의 읽기 전용 점검을 할 수 있다. 출력은 경로와 판정 결과만 남기고 개인정보와 원시 데이터를 노출하지 않는다.
- 다른 작업이나 채팅의 리팩터링을 적용할 때는 먼저 커밋, diff, 작업 트리를 확인한다. 현재 main과 비교하지 않은 채 병합하거나 기억만으로 재작성하지 않는다.
- 범위를 넓히는 선택이 필요하면 안전한 진단과 준비까지만 진행하고 사용자에게 결정을 요청한다.

## 기본 작업 흐름

1. AGENTS.md와 AGENT_MEMORY.md를 읽고 git status와 현재 빌드를 확인한다.
2. 증상을 재현하고 실제 소유 코드, DOM, CSS 또는 데이터 경로를 찾는다.
3. 가장 작은 완결된 수정으로 원인을 제거한다. 같은 기능의 중복 경로나 임시 우회는 함께 정리한다.
4. 변경 위험에 맞는 표적 QA를 먼저 실행한다. 실패를 단순 재시도하지 말고 재현 조건과 타이밍 경합을 구분한다.
5. 공개 앱 변경이면 빌드 메타데이터와 변경된 자산 버전을 맞춘다.
6. 새로 생긴 영구 원칙이나 함정이 있으면 AGENT_MEMORY.md를 같은 릴리스 커밋에 포함한다.
7. git diff --check와 최종 QA를 통과한 뒤 변경 파일만 커밋하고 main에 반영한다.
8. Pages 실행이 정확한 HEAD를 배포했는지 확인하고 공개 index와 버전 자산을 캐시 우회로 검증한다.

배포 확인 번호를 기록하기 위한 사후 문서 커밋은 만들지 않는다. 정확한 커밋과 Pages 실행 번호는 필요할 때 GitHub에서 조회하고 최종 보고에만 남긴다. main 푸시 후 60초 안에 해당 HEAD의 Pages 실행이 생기지 않으면 설정을 확인한 뒤 수동 빌드를 한 번만 요청한다.

## 빌드와 릴리스

- src/core/build.js가 공개 빌드 버전과 자산 버전의 기준이다.
- 공개 런타임 변경은 release version, build 자산 버전, index.html의 build.js 쿼리를 올린다.
- src/app.js가 바뀌면 app 자산 버전과 index.html의 app.js 쿼리를 함께 올린다.
- CSS가 바뀌면 해당 CSS 자산 버전과 index.html 쿼리를 함께 올린다.
- 문서와 테스트만 바뀐 경우 앱 빌드 번호를 올리지 않는다.
- rulesChanged는 해당 릴리스에서 Firebase 규칙이 실제로 바뀐 경우에만 true로 둔다.
- 릴리스 이름은 변경의 핵심 결과를 설명해야 하며 과거 기능 이름을 재사용하지 않는다.

## 검증 기준

- 문서 전용 변경: 문서 간 참조 확인, git diff --check, 앱 빌드 비변경 확인
- JS 또는 CJS 변경: 변경 파일마다 node --check
- 일반 앱 변경: npm.cmd run verify와 npm.cmd run qa:audit
- 대진, 점수, 진출자, 결승, 결과 변경: qa:result, qa:match, qa:operator
- 운영권, 복구, LIVE 변경: qa:operator, qa:surface와 관련 match 시나리오
- 관리자 또는 선수 DB 변경: qa:admin과 관련 surface 시나리오
- Firebase 규칙 변경: qa:rules와 qa:all을 모두 통과한 뒤 규칙과 앱의 배포 순서를 정한다.
- 공개 앱 릴리스: 최종적으로 npm.cmd run qa:all 통과가 필요하다.

브라우저 QA는 기본적으로 1365x900, 390x844, 320x720을 포함한다. 화면 변경이 없으면 불필요한 시각 테스트를 추가하지 않는다.

## Firebase와 민감정보

- 운영 데이터 쓰기는 정확한 대상 경로를 확인하고 필요한 노드를 복구 가능한 위치에 백업한 뒤 수행한다.
- 활성 대회, pending claim, 유효 lease를 먼저 확인하고 넓은 경로를 일괄 정리하지 않는다.
- 규칙과 앱 프로토콜이 함께 바뀌면 호환 순서와 롤백 경로를 먼저 정한다.
- 인증 토큰이나 자격 증명이 출력될 수 있는 명령은 사용하지 않는다. 로그인 상태는 토큰을 노출하지 않는 최소 정보로 확인한다.
- 비밀값, 인증 코드, 개인 계정 정보, 원시 Firebase 내보내기를 저장소, 메모, 보고서에 기록하지 않는다.
- 공개 LIVE 데이터는 명시적 허용 목록 DTO만 사용하며 비공개 대회 상태를 포함하지 않는다.

## 대회와 운영권 불변식

- 인증된 운영자의 쓰기 권한은 Firebase 서버 lease 하나만 기준으로 한다.
- 레거시 state.tournament.operationLock은 인증된 서버 lease 소유자를 차단할 수 없다.
- 진행 중 쓰기는 경기장, 대회 ID, registry generation, fence token과 sequence가 정확히 일치해야 한다.
- 새로고침 복구, 운영권 가져오기, 종료, 자동 종료, 되돌리기는 같은 대회 인스턴스를 확인한 뒤 실행한다.
- public viewer는 읽기 전용이며 운영자 상태를 복구하거나 게시하지 않는다.
- 지연된 쓰기는 최신 라운드나 결승 상태를 과거로 되돌릴 수 없다.
- 진출자가 없는 라운드는 정상적인 종료 결과다.
- 강제 조 편성은 실제 첫 생성 단계에만 적용되며 이후 단계로 전파되지 않는다.

## UI 원칙

- 운영 화면은 홍보 페이지가 아니라 빠른 의사결정 도구다.
- 현재 상태와 다음 행동을 먼저 보여 주고, 설명과 상태 패널과 버튼을 중복하지 않는다.
- 설정과 기타 영역은 정적 섹션으로 유지하고 불필요한 접이식 도구함을 만들지 않는다.
- 같은 스택의 프레임 가장자리, 간격, 버튼 높이와 패딩을 맞춘다.
- 모바일 운영 도크는 네 개의 동일한 열을 유지하며 기본 동작 문구는 한 줄에 들어가야 한다.
- 기존 UI를 고칠 때 소유 DOM과 기본 CSS를 수정하고 넓은 후행 important 덮어쓰기를 추가하지 않는다.
- 세부 시각 계약은 UI_REDESIGN_BRIEF.md를 따른다.

## Git과 파일 관리

- 작업 브랜치는 codex/ 접두사를 사용한다. 배포 전 현재 브랜치와 origin/main의 정확한 SHA를 확인한다.
- 관련 파일만 stage하고 사용자의 다른 변경과 추적되지 않은 파일을 보존한다.
- .codex-remote-attachments/, DESIGN_OUTPUT.md, FIREBASE_REPORT.md, QA_REPORT.md는 사용자가 별도로 요청하지 않는 한 수정하거나 커밋하지 않는다.
- git reset --hard, 무단 checkout 복원, 광범위 삭제를 사용하지 않는다.
- 생성된 node_modules와 임시 QA 산출물을 커밋하지 않는다.

## 문서 역할

- AGENTS.md: 버전과 무관한 영구 작업 규칙
- AGENT_MEMORY.md: 압축된 현재 상태, 최근 영구 변경, 함정과 후속 목표
- UI_REDESIGN_BRIEF.md: 버전과 무관한 UI 설계 계약
- TASK_QUEUE.md: 사용자가 승인했지만 아직 끝나지 않은 실제 작업만 기록
- IMPLEMENTATION_REPORT.md: 현재 구조와 안전장치의 개요

같은 빌드 정보와 배포 이력을 여러 문서에 복제하지 않는다. Git에서 확인할 수 있는 장기 커밋 목록을 메모에 누적하지 않는다.

## 자동화

- 중단된 데일리 체크는 사용자가 명시적으로 재개하라고 요청하기 전까지 재개하지 않는다.
- 반복 점검, 알림, 모니터링은 사용자의 명시적 요청 없이 새로 만들지 않는다.
